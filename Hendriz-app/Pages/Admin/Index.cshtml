@page
@model Hendriz_app.Pages.Admin.IndexModel
@{
    Layout = "_admin";
    ViewData["title"] = "hendriz";
}
@{
    for (int i = 0; i < Model.name.Count; i++)
    {
        <p style="flex-grow: 2;">hello this is dummy @ViewData["title"] @i @Model.id[i]  @Model.name[i]</p>
    }
}